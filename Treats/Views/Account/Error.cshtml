@{
  Layout = "_Layout";
}

@using Treats.ViewModels

@model RegisterViewModel

<h4><font color="red">Invalid password, please see requirments below.</font></h4>
<h2>Register a new user</h2>
<p>Please use at least 1 uppercase letter, 1 lowercase letter, 1 number, and 1 special character. Password must be at least 6 characters long.</p>
<hr />
@using (Html.BeginForm("Register", "Account", FormMethod.Post))
{
    @Html.LabelFor(user => user.Email)
    @Html.TextBoxFor(user=> user.Email)

    @Html.LabelFor(user=> user.Password)
    @Html.PasswordFor(user=> user.Password)

    @Html.LabelFor(user=> user.ConfirmPassword)
    @Html.PasswordFor(user=> user.ConfirmPassword)

    <input type="submit" value="Register" />
}
